export struct QuotaRow {
    id: string,
    label: string,
    usage-label: string,
    used-ratio: float,
    reset-label: string,
}

export struct ChartPoint {
    value: float,
}

export struct SessionRow {
    id: int,
    label: string,
    tokens-label: string,
}
