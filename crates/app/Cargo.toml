[package]
name = "tokenmaster-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
tokenmaster-platform = { path = "../platform" }

[dev-dependencies]
tempfile.workspace = true
