# TokenMaster release exit plan

The per-cycle delivery rail until the first accepted release. Repository contracts and
acceptance documents remain authoritative.

## Current milestone

One downloadable, deterministic, unsigned Windows x64 portable `0.1.0` under Apache-2.0.
Not a stable release and not signed.

Acceptance is a stranger, not a receipt: a machine that has never built this repository
downloads the packaged executable, launches it, and sees its own Codex history with a
lifetime total and a quota board that states its evidence honestly -- filled where the
runtime could reach Codex, and degraded with a named reason where it could not.

**The quota clause was narrowed deliberately, and the price is written down.** It read "a
working quota board", which this product cannot promise on a machine whose Codex is an npm
install, because every mechanism that would find the binary there is closed by a contract
this repository already holds. The entry below carries the measurement; the reason the
narrowing is legitimate rather than convenient is that the board no longer lies about it.

Before any parity claim, every row in `docs/FEATURE_PARITY.md` must be implemented or
explicitly rejected under its normative rationale.

## Sequence

Do not start a phase before the previous one meets its criterion.

| Phase | Outcome | Criterion |
|---|---|---|
| P0 | Unblock the gate | 17 workspace members; no `windows-gnu`; verify job under 45 min |
| P1 | Correctness gate | A re-classified fork keeps its proved replay prefix |
| P2 | Close the schema one-way door | Byte-exact `sqlite_master` fixture holds; one version past `USAGE_SCHEMA_VERSION` rejects as `SchemaTooNew` |
| P3 | Product surface | **met** — all four checks below |
| P4 | **Tag `v0.1.0`** | The stranger test above |
| P5 | Agent access | `tm today` equals a direct SQL sum, under 500 ms warm |
| P6 | Ingestion, capped | Cold import under 300 s median with a byte-identical selected-event set |
| P7 | Claude provider | Two `provider_id` values under one writer lease |

### P3, checked rather than assumed

Every clause of the criterion now has evidence, and two of them had none before.

- **All 11 routes reachable.** Verified from outside the process against the running
  build: the accessibility tree exposes eleven route buttons, each was activated through
  its own invoke action, and the shell header followed to the named route in ten cases.
  `Compact Widget` replaces the shell, so it was confirmed by the window resizing from
  2122x1591 to 866x1191. Activation used `InvokePattern` rather than a synthetic click --
  those did not land because the window was not foreground, which is a limit of the
  harness and not of the product -- and it runs the same callback a click does, on
  controls that each own a rectangle inside the window.
- **No cost renders with three or more decimals.** Guarded by a sweep over every micro
  value up to a dollar, then powers of ten and their neighbours to `u64::MAX`. It failed
  on its first run: `format_usd_micros` added its rounding term before dividing and
  overflowed near the top of the range, which a release build would have wrapped into an
  enormous cost rendered as a trivial one. Fixed and pinned.
- **Unavailable and legitimate zero render differently.** Both formatters behaved and
  nothing held them to it. Now pinned, and verified by breaking it.
- **A lifetime total exists.** Built across store, query, product, projection and shell;
  the header reads `16,345,419,812 all time` on a live window.

### P4, measured against the shipped package

Run against `dist/TokenMaster-0.1.0-windows-x64-unsigned.zip` built at the commit it
reports, extracted where nothing was built, with `scripts/stranger-acceptance.ps1`. These
are the numbers the `v0.1.0` tag carries, from the run against that tag's own package --
an earlier run's figures stood here until the tag was cut, which is how evidence and the
thing it certifies drift apart.

- **The package is the commit.** Its receipt commit equals `HEAD`, and
  `validate-product-package.ps1` passes. Rebuild before tagging or the receipt stops
  matching.
- **A stranger sees their own history.** `Tokens 335,518,571` and `Events 2,319 events`,
  identical at 60, 120 and 180 seconds -- filled and never falling back to dashes.
- **The lifetime total is live.** `29,335,964` at launch, then 4.60, 11.78 and 11.92 billion
  as the import runs.
- **An observed empty day reads as a zero, not as missing.** `Tokens 0` at launch, beside
  `Events 0 events`.
- **The quota board states its evidence honestly.** `Degraded: quota_discovery` at all four
  marks, where it previously showed a green `Ready` with no reason beside
  `Quota evidence unavailable`.
- **Runtime libraries come from the package**, all three, with nothing borrowed from
  System32.

**The script can now fail on the clause it exists to guard, and could not before.** It read
the header and the Today card and never looked at the state pill, so every earlier run
reported a pass while the card claimed a finished answer. It now refuses exactly one
combination -- `Ready` beside a board saying it has no evidence -- and that decision was
exercised with both states actually observed here, the pre-fix `Ready` and the post-fix
`Degraded: quota_discovery`, plus the filled-board case a machine with Codex would produce.

## Open, and waiting on the owner

One, and it is deliberately one rather than the three routes this section first offered --
those asked the owner to pick a mechanism, which is not theirs to pick.

- **Does the handoff become the product's default appearance, with the other five token sets
  remaining engineering derivatives, as they already are today?** The "as they already are"
  is the part that was missing when this was three routes: no set matches the drawing now
  either -- `Refined dark` opens `#0D0F13` against the handoff's `#090D13` for the same window
  -- so five generated sets beside one drawn one is an improvement on six undrawn ones, not a
  compromise. **Yes** and there is nothing to escalate: the drawn set replaces `Refined dark`,
  the missing roles are derived from each set's own seeds, and a letter to the designer for
  the other five goes out in parallel blocking nothing. **No** and the real fork appears,
  between waiting for the designer and reducing what the product offers -- and the second
  carries a price worth stating before it is chosen, because it reopens `P4-C` and `P4-D` in
  the specification, moves two ADRs, and needs a settings migration that **destroys the skin
  and colour-scheme a user has already chosen**. `docs/UI_REFERENCE.md` carries the counting
  and the correction to it.

## Recorded, with a phase

Defects found while working on something else. Each is deferred deliberately, not
forgotten; the phase is the commitment. Background and mechanism are in
`docs/SLINT_NOTES.md`.

**Anchors are symbols, never line numbers.** Every line reference this table once carried
had drifted out of date, and two entries described defects that had already been fixed —
a stale rail is worse than no rail, because it is trusted. A function or property name
survives edits and is greppable.

| Phase | Item | Anchor |
|---|---|---|
| **P3 exit, closed** | A lifetime total now exists and is on screen: the shell header reads `Local usage intelligence · Dashboard · 16,345,419,812 all time`, confirmed on a live window at 2122x1591. It travels the full chain rather than around it -- an unbounded aggregate over `usage_time_rollup` in the store, `LifetimeUsage` in the query layer, its own `ProductSnapshot` section, a projection field, and a shell property, because every number this interface shows arrives by snapshot. | `fn lifetime_totals`; `shell-lifetime-tokens` in `main.slint` |
| **P4 blocker, closed** | The Today card filled with real numbers and then went back to claiming no evidence while ingestion continued. Cause: a refresh publishes its status first and its usage answers second under one attempt number, and invalidation raised the section's attempt to the status's, so `classify` met an equal number and coalesced -- the analytics computed in that very refresh was discarded. During a first import the dataset identity changes every pass, so the card never refilled. The section now keeps its own attempt; an answer against the superseded dataset is still refused by the compatibility predicate, where that check belongs. Verified on a fresh extraction of the shipped package at three marks: `Tokens 197,189,455` and `Events 1,412 events` at 60, 120 and 180 seconds, with `Cost $21.83` appearing, while the lifetime total climbed from 4.46 to 11.64 billion. The same marks read two dashes before the fix. | `fn invalidate_if` in `crates/product/src/reducer.rs` |
| **P4, decided** | **No location this code could guess would fix the quota board, and every non-guessing route is closed by a contract this repository already holds.** Quota is not read from the archive; the runtime spawns `codex.exe app-server --stdio`, discovery reads `PATH` and nothing else, and `codex` on `PATH` here is only the npm shims. Four `codex.exe` exist at three versions: `%LOCALAPPDATA%\OpenAI\Codex\bin` holds `0.130.0-alpha.5`, `~/.codex/.sandbox-bin` and `~/.codex/plugins/.plugin-appserver` hold `0.146.0-alpha.3.1`, and the npm package vendors `0.144.1`, which is what `codex --version` answers. A fix searching the `%LOCALAPPDATA%` directory was written, pinned by contract and **reverted** -- it selects `0.130.0-alpha.5`, on which `codex login status` fails with `invalid type: map, expected a string`, so the guess lands on the one binary that cannot work. Following the shim means running `node` with `codex.js`, which the documented script and package-wrapper rejection forbids. Configuration is what is left, and its price was measured rather than assumed: `spec/API_CONTRACT.md` states `ApplicationEnvironment` captures only the executable, `LOCALAPPDATA`, `USERPROFILE` and `CODEX_HOME` and that production accepts no general environment-selected path, so an environment variable contradicts a binding spec; the settings profile holds **no string or path field at all**, so a persisted path means `SETTINGS_SCHEMA_VERSION` 7 to 8 through a hand-written ladder with frozen wire snapshots and three production call sites of `PortableSettings::new` that would otherwise reset it silently; `spec/SECURITY.md` forbids retaining absolute paths in exported surfaces, so it could not live in the portable half; and one new Settings label moves a closed msgid array, the literal `518` in ten assertions, and two hand-edited catalogs. **Decision: not before the tag.** The plan's own abort rule says a migration ladder never holds the first release hostage, and the criterion is narrowed instead -- legitimate only because the board now names why it has nothing, which is the entry below. | `fn resolve_current_command` in `crates/runtime/src/quota/config.rs`; `SETTINGS_SCHEMA_VERSION` in `crates/state/src/settings/value.rs` |
| **closed** | **The Plan Usage card claimed a finished answer to a question that was never successfully asked.** On a live window it rendered a `Ready` pill with no reason codes beside `Quota evidence unavailable`. Quota is the one Dashboard figure that does not come from the archive -- the runtime spawns Codex and asks it -- and the archive query succeeds either way, finding no windows, so the section reported Ready on a query that answers nothing about whether the question was ever asked. **The first fix was incomplete and the live window caught it.** A failed poll arrives by two routes: the runtime may be unobservable, which is the observation error, or it may be perfectly observable and reporting that its last poll failed, which is the health's own `quota_failure`. A discovery failure -- no executable found at all, which is this machine -- takes the second route, and the desktop crate did not read that field anywhere. Reading only the first route passed its own test and left the card unchanged in front of a user. Both routes are read now. Each half was red first (`left: Ready, right: Ready`, then `expected quota_transport, received []`) and each was verified by removing only that half and watching its own assertion fail again. | `fn map_plan_usage` in `crates/desktop/src/dashboard.rs`; `a_failed_quota_poll_reaches_the_card_that_reports_quota` in `crates/app/tests` |
| **closed** | The Today card reported `Cost $0.00` beside `Tokens --` for the same empty day. `AggregateTokenValue::from_store` asked `known_count == 0` before `known_count == event_count`, and that question cannot tell an observed empty day apart from a day whose events carry no token evidence -- only `event_count` separates them. The section is `Ready` in this state, so the answer is complete rather than mid-import, and the event count and the cost both already called the zero known: two of three agreed and only tokens dissented. `event_count == 0` is now asked first and answers `Known(0)`. A day with events but no token evidence still answers `Unavailable`, and a section with no answer yet is still `Waiting` reporting `None`. The contract that pinned the old asymmetry was rewritten rather than deleted, and the release criterion that an absent value and a measured zero must not render alike is untouched -- `Known(0)` formats as `0`, unavailable as an em dash, and that pairing is pinned by its own test. Red first: `left: Unavailable, right: Known(0)`. | `fn from_store` for `AggregateTokenValue` in `crates/query/src/analytics.rs`; `an_observed_empty_day_is_a_known_zero_and_missing_evidence_is_not` |
| post-tag | **The designer delivered a full handoff and it is now the standard, superseding both earlier drawings.** `docs/design/` holds the specification, the 1400x900 target, two 2x captures and the source app it re-composes; `docs/UI_REFERENCE.md` no longer transcribes it, it measures the distance to it. That distance, checked against the code rather than estimated: the sidebar is six items plus Settings and Help while `ProductRoute::ALL` is eleven, so History, Notifications and Compact Widget have no drawn place -- a product decision, not a drawing one. The window is fixed at 1400x900 with **nothing scrolling** and only the trend row elastic, against a Dashboard that wraps its content in a `ScrollView` sized `max(self.height, content.min-height)`. Three cards do not exist at all: Live Sessions with branch, context percentage and tool-call chips; an Activity heatmap of 7 days by 24 hours against the product's eight rows of something else; and a continuous 1-to-90-day SCALE slider that re-scopes the trend. **The heatmap's grid does not exist, and this entry said it did.** `USAGE_RHYTHM_HOURS` is 24 and `USAGE_RHYTHM_WEEKDAYS` is 7, but they bound two marginals -- `UsageRhythm { hours, weekdays }` is thirty-one buckets, not a hundred and sixty-eight, and the busiest hour beside the busiest weekday does not say Tuesday at 03:00. The inference from two true constants to a grid was mine and went into two documents before the type was read. The history request does ask for rhythm over thirty days, so the query path and its bounds are there; the missing piece is an hour-of-weekday bucket -- and that is smaller than it sounds, because `rhythm_segment` in `rhythm_sql` already carries `hour_index` and `weekday_index` on every row and the query simply groups by one of them at a time. A joint grid is a key series of 168 and a join on `weekday_index * 24 + hour_index`, not a schema change. Two font families must be bundled. Every number in the handoff is mock data: the layout is the specification and the values come from the archive. | `docs/design/README.md`; `docs/UI_REFERENCE.md`, its "distance from here" section |
| post-tag | **Let the user say where their Codex is.** The decision above defers this, not cancels it: `CodexQuotaRuntimeConfig::with_executable` already accepts an explicit path, already wins over discovery, and already validates it -- exact `.exe`, no reparse point, canonicalised, no shim -- and an invalid one is refused without falling back, which a contract pins. Only a way to set it is missing. The shape the code already supports: one accessor on `ApplicationEnvironment` and one parameter on `finish_live_bundle`, whose four callers all hold the environment in scope, with `OptionalRuntime::start` absorbing a bad path into a degraded runtime rather than a failed launch. What makes it post-tag is everything around that seam -- a settings schema step, a first-ever string field, a security invariant about retained absolute paths, and a closed localization set -- all costed in the entry above. | `fn with_executable` in `crates/runtime/src/quota/config.rs`; `fn finish_live_bundle` in `crates/app/src/application.rs` |
| **closed for `product`, open for `query`** | **The tolerance was smaller than the program's own noise, so a correct run failed at random.** Measured on CI run 30529480389, whose tree was green in its three other runs: private bytes across the 32 samples of a correct run spanned 3,035,136 to 5,988,352, a swing of **2,953,216 bytes**, against a tolerance of 2,097,152. It missed by 319,488 bytes on the last of four windows while handles, threads, user objects and GDI objects sat frozen at 105, 4, 1 and 0 through every sample -- one failure in four CI runs of identical bytes. One constant was doing two jobs: detecting the warmup plateau, where tightness is the point, and judging return, where it was below the noise floor. They are now separate, and the number that does the work turned out not to be either of them. `PRIVATE_PLATEAU_TOLERANCE` stays at 2 MiB; `PRIVATE_RETURN_TOLERANCE` is 5 MiB; and `MEASURED_ROUNDS` went from 16 to 64, which is what makes a leak visible at all. **The first attempt at this entry was wrong and the review bot caught it**: at 16 rounds a leak of one retained 4 KiB page per open reaches only about 1.7 MiB of drift, under the old bound as well as the widened one, so raising the ceiling had removed a guard without replacing it. Noise does not grow with rounds and a per-round leak does, so the rounds are the fix. Measured on the shipped configuration over sixteen known-good runs: worst window drift 3,067,904 against that leak's last window at 7,995,392, and the bound sits near the geometric middle at 1.7x clear of the noise and 1.5x under the leak. Windows rather than medians, because the same runs separate 2.6x on the worst window and only 1.4x on the median -- a leak lifts every late window while noise spikes one. The criterion moved out of the live test into `private_bytes_returned`, so it can be shown to reject rather than only observed to pass: it is checked against the recorded readings of that failing run and against three growth series. Both halves were broken on purpose -- making the criterion always accept fails the leak case, and widening the plateau tolerance to the return tolerance fails `bimodal_warmup_with_repeated_low_outliers_is_not_a_plateau`, which is what proves the split guards something. **The `crates/query` sibling is untouched and still carries the original shape.** | `fn private_bytes_returned` in `crates/product/tests/resource_contract.rs`; the untouched `resource_contract` in `crates/query/tests` |
| post-tag | **A resource contract fails when the machine is busy, which makes it a gate that can be talked out of.** `resource_contract` in `crates/query` demands a topology-stable retained plateau within 64 rounds and reads private bytes to find it. Run beside a packaged build and a DPI capture it saw private bytes swinging between 7.20 MB and 3.40-4.61 MB while handles, threads and user objects never moved, and gave up: `did not establish a topology-stable retained plateau within 64 rounds`. Re-run alone on the same commit it passes. The measurement is therefore load-sensitive rather than wrong, and that is the problem: a check that fails for a reason unrelated to the code teaches its reader to dismiss it, and the next dismissal will be of a real regression. Handles and object counts held perfectly steady throughout, so the plateau condition is the part to reconsider -- either bound it to the counters that are actually stable, or make the byte plateau tolerant of an allocator that has not returned pages yet. | `resource_contract` in `crates/query/tests`; the plateau loop that reports `samples=[...]` |
| **closed** | **The localization contract checked one direction only, and a shipped string was already through the hole.** It asserts that every entry of its closed msgid arrays appears as `@tr(...)` in the Slint tree. There is no assertion the other way -- that every `@tr` in the tree is in an array -- so a label added to a view and forgotten in the catalog passes. One already has: `@tr("Alerts")` renders in the shell tab strip while `msgid "Alerts"` exists in neither `translations/ru` nor any contract array, which means it ships as English in Russian today. The reverse assertion now walks the `ui` tree from disk -- from disk, because a list of sources would have to be remembered and forgetting is the failure it exists to catch -- and requires every `@tr` to have an entry in both catalogues. Red first with exactly one name, then green, then verified by deleting the entry again. Two guards stop it degrading: fewer than eleven sources or fewer than a hundred literals is itself a failure. One correction on the way, recorded in the code: the catalogues are CRLF, so a match anchored to a bare newline named all 583 literals as missing, and a test that cries about everything teaches its reader to skip it. | `crates/desktop/tests/localization_contract.rs`; `@tr("Alerts")` in `crates/desktop/ui/main.slint` |
| post-tag | **Two font families the design requires are absent, and the type token points at neither.** The handoff specifies JetBrains Mono for every number and technical label and IBM Plex Sans for navigation and prose, both bundled for an offline build. `UiTokens.mono` is `"Consolas"`, there is no proportional-family token at all, and nothing in the crate registers a font. Every size in the spec's scale also collides with the existing density-scaled steps, and the fifteen colour roles in `UiPalette` cannot express the spec's separate chrome, rail, hero and inset surfaces or its six border tones. **The design is not reachable by restyling; it needs the token system widened first.** | `UiTokens.mono` in `crates/desktop/ui/tokens.slint`; `fn ui_palette` in `crates/desktop/src/skin.rs` |
| **closed** | **Eight Activity labels shipped untranslated in every locale, outside the contract by construction.** `activity_label` in `crates/desktop/src/ui.rs` returns `Read`, `Edit / Write`, `Search`, `Git`, `Build / Test`, `Web`, `Subagents` and `Terminal` as bare `&'static str` from Rust. The contract asserts a closed msgid set and that each entry appears as `@tr(...)` inside the Slint tree -- so a string emitted from Rust escapes it **by construction**, not by oversight. That is the same shape as three other findings this cycle: a gate of which only a third was run, an acceptance script that could not fail on its own clause, and a resource check that fails for reasons unrelated to the code. Fixed by removing the English path rather than wrapping it: the helper is gone, the `label` field is gone from `DashboardActivityRow`, and the card resolves the stable key through `ProjectionStrings` like every other keyed string. Wrapping would have fixed the eight and left the next category free to arrive the same way. The reverse assertion above now covers them, since they are `@tr` in the tree. | `fn activity_label` in `crates/desktop/src/ui.rs`; the closed-set assertions in `crates/desktop/tests/localization_contract.rs` |
| post-tag | **Live Sessions asks for facts the whole workspace does not hold.** Mapped crate by crate rather than guessed: the branch name is parsed by the Codex reader and rides only inside an opaque resume blob, with no column in `usage_observation` or `usage_event` and no query accessor. Context percentage, tokens remaining and "at limit" exist nowhere -- `context_window` appears only in the parser and is the model's window size, not consumption. Per-tool names and counts live in `ParserState::tool_counts` and never leave `crates/codex`; the store keeps eight aggregate activity columns instead. Session liveness has no concept in any crate. The originator is persisted but the query layer never exposes it, and there is no session-to-repository join at all. **So this card is ingestion and schema work before it is UI work**, and it is the one part of the handoff that cannot be reached by carrying existing data. | `ParserState::tool_counts` in `crates/codex/src/parser/state.rs`; `usage_event` columns in `crates/store/src/usage/schema.rs` |
| **closed** | **The Dashboard's cost per hundred added lines was arithmetically wrong whenever a project had more than one repository.** `map_code_output` accumulates `efficiency_usage.checked_add(value.usage_cost().get())` over every repository, but that cost is the **project's**, not the repository's: `map_repository` fills it from `projects[project_index].1`, so two repositories sharing an alias carry the identical value and summing them multiplies it. The Projects route knows this and refuses -- `ProjectEfficiencyAccumulator::add_available` raises `efficiency_evidence_mismatch` if two costs differ and never sums them, adding only lines. So the two screens report different numbers for the same repositories, and the Dashboard's is N times too large for a project of N repositories. `map_code_output` also ignores `usage_dataset_identity` -- which was recorded here as a second defect and is not one. `git_output` resolves the identity **once per response**, from the single publication behind it, and hands the same value to every repository, so two repositories of one project cannot disagree about it and the blending this sentence used to describe cannot occur. The check in `ProjectEfficiencyAccumulator` is defensive against a producer that does not exist yet, not a rule the Dashboard is breaking. The claim was made from counting the identifier in two files, 0 against 2, without reading where the value comes from; the cost half of that same accumulator is the half that mattered, and it is fixed above. Both contract fixtures use a single repository, so the fan-out is uncovered. Measured before fixing: one archive with two repositories of one project answered 6,667 micros on the Dashboard and 3,333 on Projects. Each cost is now counted once per alias while lines still sum. The contract asserts the two screens **against each other** rather than against a constant, because one archive cannot owe two answers and a figure written twice can be corrected once; red first at that same pair, then verified by restoring the sum. Left open deliberately: the query layer computes the correct per-repository figure as `GitEfficiencyValue::cost_per_100_added_lines` and **nothing in production reads it**. Substituting it here would swap one wrong number for another, since the Dashboard aggregates across repositories and that value does not -- so it stays a separate item rather than a change made in passing. | `fn map_code_output` in `crates/desktop/src/dashboard.rs`; `ProjectEfficiencyAccumulator::add_available` in `crates/desktop/src/projects.rs` |
| post-tag | **The trend chart's markers do not sit on its line.** The polyline is emitted from Rust into a 1000x100 viewbox with `x = 28 + i/(n-1) * 944` and a band from y 10 to 80, while the dots, hover crosshair, tooltip anchor and axis labels are placed by a Slint overlay at the centre of an equal-stretch cell, `(i + 0.5)/n`, over a band of `parent.height - 26px`. Two independent statements of where point `i` sits, in different coordinate systems: about one percent of chart width apart at both ends and several pixels apart vertically, so the crosshair annotates the wrong column at the edges. A third statement of the same geometry is `TREND_ZERO_LINE = 80.0`, written as a literal where it is `10.0 + 70.0` from the y formula, so editing the band silently detaches every area fill from its line. | `fn dashboard_trend_path` and `const TREND_ZERO_LINE` in `crates/desktop/src/ui.rs`; the marker overlay in `dashboard-view.slint` |
| post-tag | **The reminder preset lead set is written three times in one file** -- a function-local `PRESETS` array, five separate `leads.contains` calls, and a fourth literal list in the custom-row filter. Disagree and a lead is shown both as a ticked preset and as an editable row, where `dedup` on save silently reverts the user's edit, or it is dropped from both and lost. A coupled sub-instance: `reminder_custom_rows` chooses days whenever the lead divides by 86,400 while `format_reminder_lead` requires at least two days, so one value renders as `1 day` in the editor and `24h` in the summary. Unreachable today only because the preset filter strips it. | `fn reminder_policy_intent` and `fn apply_reliable_state` in `crates/desktop/src/ui.rs` |
| post-tag, **this entry was wrong** | Test helpers wait on a background runtime by spinning on `thread::yield_now` rather than sleeping between polls, which starves the thread being waited on when the machine has fewer cores than the test binary has parallel cases. **This entry used to close by calling it an efficiency item and not a correctness one, on the grounds that every hang bound was thirty seconds. Both halves are false.** Counted rather than assumed: sixteen of these waits are bounded at thirty seconds and **five are bounded at two**, in `notification_tests.rs`, `desktop/src/controller.rs` and `runtime/src/quota/runtime.rs`. One of the two-second ones decided a gate run. **A second correction, to this correction: the recovery contracts were not starved by spinners in sibling test binaries, and that sentence stood here for an hour before the log was read.** `cargo test` runs test binaries one at a time -- every `Running <binary>` line in a gate log is closed by its own `test result:` before the next begins -- so there is no cross-binary contention to be had, and the recovery fixture sleeps rather than spins. What that binary does do is run fifty-seven cases in parallel while many of them **spawn a whole child test executable** doing durable SQLite work, so the load is its own. Spinning is therefore an intra-process problem, real where a binary runs eighty-two cases beside the worker they are waiting on, and **not** the explanation for the 1656-second recovery run, which remains unexplained and must not be given a cause it has not earned. **The obvious fix was tried and reverted, and its failure is the useful part.** Replacing the spin with `sleep(10ms)` across twenty-one files compiled clean and then broke `scheduling_and_callback_failures_release_the_lease`, which read `(2, 0, 1)` where it wanted `(1, 0, 1)`: its coordinator ticks every five milliseconds, so a ten-millisecond poll no longer lands inside the window between the awaited transition and the assertion. That is not a one-file accident -- the tightest interval in each of the twenty-one files is **zero to ten milliseconds**, at or below any sleep that actually releases a core, and Windows timer granularity puts a floor under how short the sleep can be. So polling cannot be made both core-friendly and tight enough here: **the waits have to stop polling.** A transition these tests can block on -- a channel send or a condvar -- is the shape that fixes both starvation and the observation window, and it is real work rather than a substitution. `backup_ui_latency_contract` keeps its spin deliberately: it measures elapsed time, and sleep granularity would enter the measurement. | `fn wait_until` in `crates/app/src/notification_tests.rs`; `fn await_completion` in `crates/product/tests/runtime_status_contract.rs`, which is the one already done right |
| **P3 blocker, half closed** | The window renders nothing where the buffer was cleared: softbuffer reports a stale buffer age on Win32 and Slint's compensating `occluded()` hook is unreachable there, because winit emits no `Occluded` on Windows. Mechanism, measurements and the upstream comment are in `docs/SLINT_NOTES.md`. `fn force_full_repaint` closes the restore path — 27.7 mean luma after, against 48.4 for a move. **A window the user drags is still affected and cannot be fixed from here**: Slint surfaces no move event. Report upstream; until then the drag case stands. | `fn force_full_repaint`; upstream `i-slint-backend-winit/renderer/sw.rs` |
| post-tag, **evidence expired** | A retained scan, id 1762, once ran 2894 seconds and ended `failed` having seen 572 of 4016 sources, between neighbours that each completed in about 4 seconds. It has since aged out of the 32-row history window, which now holds scans 1826 to 1857, **all 32 of them `complete`**, the last eight lasting 1.0 to 1.9 seconds. So the anomaly cannot be investigated from the archive any longer and has not recurred across at least sixty-five scans. Kept as a note rather than a task: if a scan ever fails again the row is the place to look, and a scan is now expected to take under two seconds. | `usage_scan`, bounded by `SCAN_HISTORY_PER_SCOPE` |
| note, **closed** | **Three dashboard layout defects recorded here did not exist.** A clipped Today row, cards cut at the right edge, and a data-less card claiming full height were all produced by the measuring process, not the product: PowerShell was not DPI aware, so `GetWindowRect` returned virtualised coordinates -- 1061x796 for a window that is really 2266x1511 -- and `PrintWindow` drew the full window into a bitmap half its width, cutting the right edge. `MoveWindow` was halved the same way, which pushed the board under its 944px narrow threshold and stacked it into one column. Captured from a process that calls `SetProcessDpiAwarenessContext(-4)` first, the dashboard shows `COST`, `TOKENS` and `EVENTS` together, a two-column board, and no clipping. **Any capture tool used here must declare DPI awareness before it measures anything**, and the sidebar is the cheap check: it declares `168px` and must measure 168 logical pixels. | `scripts/stranger-acceptance.ps1` declares it; the scripts that dropped it produced every wrong reading |
| P3, **not reproduced** | A white band was recorded below the content at the bottom of the window. It does not appear in a DPI-aware capture at 2266x1511: the last rows read luma 24.9 for the tab bar and then 0.0 for the window border, with nothing brighter between. Either `fn force_full_repaint` closed it or it was the same stale-buffer artefact. Left recorded rather than deleted, because one capture at one size is not proof of absence. | capture and scan the bottom rows again after any change to the root layout, with `scripts/stranger-acceptance.ps1` |
| post-tag | `high-contrast` and `reduced-motion` are declared and consumed by the tree but never set from Rust, so both affordances are inert. Confirmed: zero `set_high_contrast` / `set_reduced_motion` call sites. | `main.slint`, `in property <bool> high-contrast` and `reduced-motion` |
| post-tag | **The Dashboard states its section order four times in one file, and one of the four already has a name.** `active-section` resolves `slot.key` to an index by a chain of conditionals -- and the two lines under it, setting `state` and `reasons` on the collapsed card, rebuild the identical chain instead of reading it. Each card then indexes `root.sections[N]` directly for a fourth statement. Nothing binds any of them to `DesktopBoardSectionKey::ALL`, which is where the order actually lives, so reordering there would silently show every card another card's state and reasons, with no compilation error anywhere. Two of the four are free to remove -- `active-section.state` and `.reasons` already exist. Removing the rest wants the section carried on the slot row from Rust, where both rows already have a `key` and the match is known; that is the first nested struct in `models.slint`, so it is a decision rather than a tidy-up. **A warning for whoever reads this next: `workbench_canonical_rank` in `ui.rs` maps the same six keys to different numbers and looks exactly like the contradiction. It is not one -- it orders the workbench, not the array -- and comparing the two tables instead of reading `ALL` nearly produced a fix to correct code.** | `dashboard-view.slint`, `out property <DashboardSectionRow> active-section` |
| post-tag | The Win32 tray carries 39 `unsafe` blocks and one SAFETY comment, and lives in the UI crate rather than `platform`, which exists to own OS FFI and documents 23 of its 25 blocks. Moving it there and then adding `#![forbid(unsafe_code)]` to `desktop` is what stops it drifting back. | `crates/desktop/src/native_tray.rs` |
| **closed** | **The gate was red for thirty-six hours across seventy-three consecutive CI runs, and every local report said green.** Three failures were stacked behind one another, because `verify-m0.ps1` stops at the first Pester file that fails and every remote run stopped in the same place. All three descend from the move to the Skia renderer, which forced the dynamic CRT and started three runtime libraries shipping beside the executable -- a fact written in four places and learned in two. That commit was cited here by hash until the squash onto `main` orphaned every hash these documents carried, which is why `CLAUDE.md` forbids them; `git log history/product-architecture --grep "ship the CRT"` finds it. `Assert-ProductPackageStage` grew the names and `validate-product-package.ps1` grew its count from nine to twelve; the two hand-built test fixtures and `validate-p3e-interactive.ps1`'s own `-ne 9` did not. The middle one is not a test defect: that validator is what an operator's receipt is measured against, so it would have rejected every genuine twelve-file package, and it was never run against one. The count now exists once and both validators read its length; the fixtures still spell the names by hand, because a fixture generated from the list under test cannot disagree with it, and disagreement is the whole content of the assertion. Red first in both suites on their real messages, with `TM-P3E-PROVENANCE` pinned to its throw site by marking all ten apart, since the validator collapses every provenance failure into one code. Then broken on purpose in both directions: a stage one file short and one file over both throw, and the P3-E validator accepts twelve entries while rejecting eleven and thirteen. **The reason it survived is not the fixture but the habit** -- the same partial-gate rule this repository's instructions were amended for a day earlier, plus a remote gate that answers on a delay and was therefore never read. | `Get-ProductPackageExpectedFile` in `scripts/product-package-lib.ps1` |
| **closed** | **No tag has ever produced a CI-built artifact, `v0.1.0` included.** `package-product.ps1` located the toolchain with `vswhere -products Microsoft.VisualStudio.Product.BuildTools`, demanding a particular Visual Studio edition on top of the `-requires` clause that asks for what the build actually needs. GitHub's `windows-2025` image ships Enterprise, so vswhere answered nothing and the release workflow died on that line every time. Nothing local contradicted it, because this machine has Build Tools installed -- the package measured for the tag was built here, not on a runner. A second defect hid the first: `.Trim()` was called on the invocation, so the null-reference threw one line before the check written to catch exactly that, and the guard's message had never once been seen. Edition filter removed, toolset requirement kept, result read before it is trimmed. Verified as far as this machine reaches -- the real locator resolves the identical path with the filter gone, a stub that prints nothing now raises the guard's own message, and a full package built and validated end to end at twelve entries with its receipt commit equal to `HEAD`. **Still unverified on a runner**, which is the open question above. | `$VsWhere` in `scripts/package-product.ps1` |
| post-tag, **worse than it looks** | **Twenty-six recovery contracts failed on one gate run and passed on the next, on identical bytes.** `recovery_adversarial_contract` reported `StoreError { code: DeadlineExceeded }` from eighteen of them, `StateError { code: Unavailable }` from the crash-resume fixtures of most of the rest, and one bare `left == right` where a launch counter had not advanced -- three faces of the same starvation -- and finished the binary in **1656 seconds**; re-run alone against the same commit, with only a markdown file differing from the green run before it, all **57 passed in 10.5 seconds**. A hundred and fifty-eight times slower is not a slow machine, it is a starved one -- these tests stage SQLite files and wait on thirty-second deadlines, so whatever saturated the disk decided the result. This is the same class as the `resource_contract` entry above and strictly more dangerous, because that one fails alone and this one fails twenty-six at a time: a wall of red that is not about the code is exactly what teaches a reader to re-run instead of read, and the next real regression will arrive dressed identically. The fix is not a longer deadline, which only moves the threshold -- it is for a deadline that expires to say whether it was starved, by reporting the work it actually completed against the time it was given. | `verified source snapshot` at `restore_contract.rs`; the thirty-second deadline in `crates/state/tests/support/recovery_crash_fixture.rs` |
| post-tag | **The package staging fixture is written twice by hand, and the CRT list four times in all.** `product-package.Tests.ps1` and `validate-p3e-interactive.Tests.ps1` each build the same twelve-file stage independently, which is what let one omission break two suites at once; the producer names the three libraries again in order to copy them, and `validate-msvc-product-binary.ps1` names them a fourth time as the imports a clean binary is allowed to carry. That last one is a genuinely different fact and should stay separate. The two fixtures are not: they want one shared helper that stays independent of `Get-ProductPackageExpectedFile`, so the closed-set assertion keeps two opinions to compare while the staging code has only one. | `New-P3eFixture` in `scripts/tests/validate-p3e-interactive.Tests.ps1` |
| **closed** | **The work is on `main`, and the way it got there is the entry.** A fast-forward was impossible: `main` refuses a direct push and a force push with `enforce_admins` on, refuses a merge commit under `required_linear_history`, and enables exactly one merge method -- squash. So the 675 commits landed as one, and every commit message carrying the measurement behind its change would have gone with the head branch, which the repository deletes on merge automatically. The head was tagged `history/product-architecture` first and a ruleset now forbids deleting or moving anything under `history/`, with no bypass actor -- verified by attempting both and being refused. The branch was gone the instant the merge landed, so that tag is the only reason the history exists. `main`'s tree is byte-identical to the tree CI verified. | `history/product-architecture`; the squash commit on `main` |
| **closed** | **The release workflow builds an artifact, for the first time ever.** Dispatched on `main` after the merge, since `workflow_dispatch` is refused on any other branch: success in thirteen minutes, producing `tokenmaster-windows-x64-unsigned` at 17,482,790 bytes. Until then it had failed at every tag including `v0.1.0`, on a `vswhere` call that demanded the Build Tools edition specifically while GitHub's image ships Enterprise -- so no tag has a CI-built download behind it, and the package measured for `v0.1.0` was built on a developer machine. The clause this closes was the last thing recorded as unverified on a runner. | `$VsWhere` in `scripts/package-product.ps1` |
| **closed** | **The four Russian strings are right, and that is an audit result rather than a preference.** `Оповещения` for Alerts is not a stylistic choice: `Notifications` is a separate msgid rendered `Уведомления`, so the common word is taken and collapsing them would merge two concepts. The three Activity categories sit in a row with `Чтение`, `Поиск`, `Git`, `Веб`, `Терминал` and match its shape. The whole catalogue was audited rather than the four lines: 528 msgids, **zero untranslated** -- the one empty `msgstr` is the gettext header -- and six entries left in Latin, each correctly so, being the product name, `Git`, a separator and three format templates. Two apparent inconsistencies were read in context and are deliberate: `сессия` appears twice against `сеанс` twenty-two times because those two are a *browser* session in a privacy statement, a different concept; and `Справка / О приложении` capitalises after the slash because both halves are menu names, where the Activity categories are not. `Сборка / тестирование` would be the purist's form and was rejected -- the Activity card is narrow by the handoff and `тесты` is what developers say. | the `ru` catalogue under `crates/desktop/translations` |
| **closed, first pass scoped** | **Live Sessions is cheaper than this plan said, because the plan looked at the wrong layer.** The entry below costed it from `usage_event` columns and concluded ingestion work. The query layer already answers it: `UsageSessionSummary` carries the session key, first and last timestamps, metrics and cost, behind `QueryService::usage_sessions`, which is what the Sessions route runs on. So the first pass -- a live list with an activity mark from the last timestamp, tokens and cost -- is **projection work with no schema migration at all**: one `ProductSnapshot` section, projection fields, one Slint card. **The model chip is not in it, and this entry claimed it was.** `UsageSessionSummary` carries the key, two timestamps, aggregate metrics and cost and no model at all, which was read before the claim was written and contradicted anyway; a model reaches a session only through the session-detail breakdown, so the chip belongs to a later pass with its own query work. Branch name, context percentage and per-tool chips remain genuine ingestion work and stay in the entry below as passes two and three. The correction is the point: a cost estimate taken from one layer without reading the one above it was too high by a whole migration. | `UsageSessionSummary` and `fn usage_sessions` in `crates/query` |
| **P4 exit blocker** | **A contract test deadlocks, and a deadlocked gate has no result at all.** `ten_thousand_scheduler_hints_create_at_most_one_engine_follow_up` in `crates/runtime/tests/scheduler_contract.rs` stopped a local gate for **two hours and twenty-four minutes** before anyone looked. It was not slow: all three threads of the test process sat in `Wait` with the process at **CPU 0.00**, so it was blocked on a lock or a channel rather than computing. Measured in isolation afterwards, **one run in fifteen** hangs; the other fourteen finish in seconds. This is worse than the load-sensitive failures recorded above and belongs in a different class: a red test reports something and the gate moves on, while this one reports nothing and the gate never ends. On CI the job carries `timeout-minutes: 75`, so the run dies at seventy-five minutes with no failing test named -- and the green runs of 30 July took about sixty minutes each against a historical thirty-two, which is unexplained and may be the same wait resolving late rather than never. **One wait was found and removed, and whether it was *this* wait is a separate question.** `RefreshWorker::wait_for_completion` blocked on `recv_timeout` while holding the mutex that guards the completion receiver -- a `std` `Receiver` is not `Sync`, so waiting on it means holding it. The same mutex is taken by `try_completion`, which has sixty-one call sites, and by the worker thread's own eviction path when the one-slot result queue is full, and production reaches this with a timeout of whatever remains until a deadline. Measured: a reader arriving behind a two-second wait was blocked for 1.91 of those seconds. Slicing the wait was tried first and measured to fail -- the platform mutex is not fair, so a waiter that releases and reacquires is handed it straight back. Replaced with a slot and a condition variable, whose `wait_timeout` releases the mutex by construction; fifteen worker contracts pass and the new one fails again the moment the slot is held across the wait. **The deadlock itself was not declared fixed on that basis, and it was right not to be: the engine slot was never this hang.** The mechanism is now known, read link by link and then reproduced on demand. This test's `release` channel is declared **before** the worker, so unwinding drops the worker first; `RefreshWorker::drop` calls `shutdown`, whose `cancel` only raises a cooperative flag the execute closure never reads, and then **joins**. The closure is parked in an unbounded `recv()` whose sender is still alive in the panicking frame, so the join never returns, the panic never completes, and the harness names no test. The trigger is the one piece of nondeterminism in the window: `submit_one` increments `submitted_count` only *after* the submit closure returns, while `pending_count` reaches 1 *inside* `RefreshWorker::submit`, so a poll that stops on `pending_count` alone can read the scheduler's counter one increment early and fail the assert that sits before `release_sender.send(())`. **Measured in both directions with that failure injected deliberately.** With the unbounded `recv`: still running at **180.2 seconds, CPU 0 seconds, three threads**, no test named -- the recorded symptoms exactly, reproduced on demand for the first time. With a 30-second bounded receive: the failure is named in **30.00 seconds**. Fixed by bounding that receive and by polling both counters under one deadline; the suite passes 7 of 7. **The sweep found the same defect next door and it was not latent in theory only.** All six `release` channels in `crates/engine/tests/worker_contract.rs` are declared before their workers and every execute closure parked on an unbounded `recv`. Injecting a failure into the parked window of `completion_wait_is_bounded_and_returns_the_exact_published_receipt` left the process running at **150.2 seconds, CPU 0, three threads**, exactly as the scheduler test did; bounded through one shared `await_release` helper, the same failure is named in **30.02 seconds**. Fifteen contracts pass and clippy is clean with warnings denied. The bound is written once, with its reason, rather than six times. | `ten_thousand_scheduler_hints_create_at_most_one_engine_follow_up` in `crates/runtime/tests/scheduler_contract.rs` |
| **closed, with a standing gap** | **The change above made the normative contract false, and nothing in the gate could have noticed.** Replacing the completion channel with a condition-variable slot rewrote a component `spec/API_CONTRACT.md` describes, and the contract still required "one capacity-one latest-only completion channel" while ADR-015 still named latest-result channels. Those documents are the behavioral source of truth this repository reads before changing behaviour, so the accepted implementation contradicted the thing the next reader would build against. Found by the review bot, not by me and not by a test. **The standing gap is the part worth keeping: no stage of the sixteen reads `spec/` at all** -- not a Pester suite, not a Rust test, and no `include_str!` of any specification file, searched across `scripts/` and every crate. A gate cannot diff prose against behaviour, so a new stage is not the answer and none is proposed; the answer is that a change to a component the contract names goes looking for the paragraph naming it, and `grep` on the type name finds it in seconds. The contract paragraph now states the two properties a caller can observe -- a wait holds no lock admission, publication or another reader takes, and a thread that ends wakes every waiter instead of leaving each to its deadline. ADR-015 was **not** rewritten: it records what was decided at P1-C.4, and editing the channel out of it would erase both the choice and the reason it was abandoned, so it gains one closing pointer and ADR-095 carries the measurement. | `RefreshWorker` in `spec/API_CONTRACT.md`; ADR-095 in `spec/DECISIONS.md` |
| **closed** | **The rule against idle animation could not see the only animation in the product.** `recovery_ui_source_keeps_authority_bounded_and_accessible` forbids `Timer {`, `animate ` and `animation-` across five Slint files named by hand -- and `animation-tick()` lives in `dashboard-view.slint`, which is not one of them. The tree has twenty-five Slint sources and that check read five. This is not hypothetical: the unguarded form of that binding shipped once and was caught by measuring **97.7% of one core with the window shown and 105.5% with it hidden**, for the life of the process, because Slint 1.17 instantiates the contents of an `if` eagerly and reading the tick schedules a repaint forever. The guard that keeps it legal was a comment. Now a second test walks every `ui/**/*.slint` from disk -- from disk for the same reason the localization contract is, a list has to be remembered and forgetting is the failure -- strips comments so neither the explanation nor a new animation can hide in one, and requires exactly one match, that it is `animation-tick()`, and that the line above it carries both `root.active` and `!root.reduced-motion`. Broken on purpose in both directions: an `animate width` added to `sessions-view.slint`, a file the old check never opened, fails on the count; replacing the guard with `(true)` fails on the guard with the wrong text quoted. | `the_only_animation_in_the_slint_tree_is_the_guarded_import_dot` in `crates/desktop/tests/recovery_ui_contract.rs`; `ImportActivityDot` in `dashboard-view.slint` |
| **closed** | **The check that pins the gate's own composition named seven suites while the gate ran eight.** `secret-scan.Tests.ps1` was absent from the hand-written list in `m0-scripts.Tests.ps1`, so deleting that stage from `verify-m0.ps1` would have turned nothing red and every later "sixteen stages" claim would have quietly meant fifteen -- the same partial-gate shape that cost thirty-six hours across seventy-three red runs, this time built into the guard against it. The list is now enumerated from `scripts/tests/*.Tests.ps1` on disk and every file found must appear in `verify-m0.ps1`, with a floor of eight so an empty enumeration cannot pass silently; a ninth suite added tomorrow is covered without anyone remembering to add it. Proven by deleting the `pester-secret-scan` line and watching the test fail, then restoring the file byte-identical. | `verification runs every Pester suite that exists` in `scripts/tests/m0-scripts.Tests.ps1` |
| **closed** | **An outside critic was asked to audit the project against its own stated criteria, and seven of its seven findings held up when checked against the code.** All were documents contradicting the product, which is the category that costs most because every later decision is made on them. `UsageReadStore` requires `USAGE_SCHEMA_VERSION`, which is 15; `API_CONTRACT` said schema-v14 twice, `DATA_CONTRACT` twice and ADR-026 once, so five sites named a version the reader rejects -- all five now name the symbol instead, which cannot drift on the next bump, while the ADR that actually introduced v14 keeps its number because it is history. `TRACEABILITY`'s TM-REL-003 still called the package a "nine-file ZIP" and `RELEASE_AUDIT_BOUNDARY` a "nine-file portable stage"; both now cite `Get-ProductPackageExpectedFile`, which is the twelve. That same boundary document required "absence of dynamic Visual C++/Universal CRT imports" -- the exact opposite of what `validate-msvc-product-binary.ps1` enforces since Skia forced the dynamic CRT, so an auditor following it would fail every genuine build. `SECURITY.md` carried a spliced sentence, "rather than the release library, and scans its strings", which the archival tag shows was born broken rather than damaged later. **Two more were found by checking those seven rather than by the critic.** The same boundary document promised a GNU evidence lane against a `rust-toolchain.toml` that pins MSVC alone -- the lane exists only in stale `reports/` logs. And `SECURITY.md` twice said "the current gate covers" with **different numbers**, 76/43/3 and 72/22/1, while `verify-dependency-policy.ps1` runs `cargo-deny` and counts no packages and no files at all: nothing produced either figure and the receipt is the only real answer, so both are gone. The critic's single imprecision was a quote it reported as missing that was merely wrapped across a line. | one `fable` reader at `high`, no fan-out; `spec/SECURITY.md`, `docs/RELEASE_AUDIT_BOUNDARY.md` |
| **closed** | **Both READMEs were narrative status documents, which this repository has deleted five of.** `README.md`'s "Current state" ran thirty-eight lines and still announced that P4 localization, later-page Sessions navigation and the secret scan were absent, after all three existed; `README_RU.md` was worse, naming P2-B as the frontier with the whole of P3 behind it, and it **ships inside the package** to users. Both now say what the product is built on and point at `spec/TRACEABILITY.md` for status and `docs/RELEASE_EXIT_PLAN.md` for order, which is where `CLAUDE.md` already said status lives. Both also taught the partial gate by example -- a five-command block with `verify-m0.ps1` last, where the first commands are stages the gate already runs -- and now carry the one argument-less command with the seventy-three-red-run incident named beside it. The secret scan stays listed separately and correctly: the gate runs its contract suite but never the scan, because a scan needs a built package. | `README.md`, `README_RU.md`; `Get-ProductPackageExpectedFile` ships both |
| post-tag | **A scheduled change lost the event that was supposed to trigger it.** ADR-024 said the workspace-global forced target would be replaced by explicit build-script target selection once a dual-lane P6 comparison passed. There is no second lane to compare any more, so that comparison cannot happen and the replacement is now waiting on nothing. `[build] target = "x86_64-pc-windows-msvc"` is still in `.cargo/config.toml`; with the toolchain pinning the same triple as host and target it is redundant rather than wrong, which is exactly why it will sit there unexamined. Decide it on its own merits: either keep it deliberately, or drop it and let the pin be the single statement of the target. Found while removing the GNU lane's other mention, not by the audit. | `[build] target` in `.cargo/config.toml`; ADR-024 in `spec/DECISIONS.md` |
| **spelling closed, two holes open** | **The animation scan matched exact bytes, including a trailing space**, so `Timer{` or `animate` followed by a tab walked past it. That half is fixed: the matcher is now the predicate `animates`, which takes each keyword independently of the whitespace around its opening delimiter and adds the `states`/`transitions` forms. Being a pure function it is tested as one -- nine spellings that must be recognised and five that must not, so the guarantee no longer depends on someone writing an evasion into a view first. Proven by restoring the byte list and watching that test fail on `Timer{ interval: 1s; }`; **the first attempt at that sabotage did not land at all and the test passed, which read exactly like a test that guards nothing** -- the `Replace` had missed on line endings, and a tool that failed to look reports in the same shape as one that looked and found nothing. **Two holes the audit found and this entry does not close.** The guard assertion reads the *raw* line above the tick while the match is comment-stripped, so a refactor leaving `// root.active && !root.reduced-motion` above an unguarded binding satisfies both assertions. And `split("//")` truncates code after a `//` inside a string literal, so `property <string> u: "https://x"; animate width { }` on one line is never scanned. The repair is to strip the guard candidate the same way and to make the strip quote-aware, each proven by planting exactly that line. | `fn animates` and `the_only_animation_in_the_slint_tree_is_the_guarded_import_dot` in `crates/desktop/tests/recovery_ui_contract.rs` |
| **closed** | **A Pester suite that ran no tests passed its gate stage, and the gate then reported PASS.** `Invoke-PesterChecked` decided a stage from `$Result.FailedCount` alone, and a suite whose tests are all commented out, all skipped, or which discovers nothing has a `FailedCount` of zero. Measured rather than reasoned, on Pester 5.7.1 against a fixture whose only `It` is commented out: `FailedCount = 0`, `PassedCount = 0`, `TotalCount = 0`, `Result = Passed`. So one of the eight suites could have quietly stopped testing anything while the gate still printed sixteen green stages -- the same shape as every other finding this round, a check that cannot fail, except this one sits inside the gate and would launder every claim made downstream of it. A missing path was safe by accident rather than design: `Invoke-Pester` throws `No test files were found`, which does surface. The condition is now `FailedCount -eq 0 -and PassedCount -gt 0`, which also catches a fully skipped suite, and the thrown message carries all three counts instead of naming only the path. **Proven end to end in both directions, not by reading the condition.** A ninth stage pointed at that fixture was inserted ahead of the first real suite and the whole gate run twice: with the old condition it printed `TM-M0-STAGE-PASS pester-BREAK-ON-PURPOSE` and went on to exit 0; with the new one it stopped there with exit 1, after clean-root, immutable-actions and dependency-policy had passed. The stage was then removed and all eight suites re-run at 75 of 75, none skipped, each between 2 and 31 tests, so the floor costs nothing. Found by reading `Invoke-PesterChecked` while the second audit ran, not by either audit. | `Invoke-PesterChecked` in `scripts/verify-m0.ps1` |
| **P4 exit blocker** | **A completion notifier that must not block takes a lock held for up to five minutes.** The trait says it outright -- "Lossy, nonblocking hint emitted after the latest completion receipt is published. Implementations must not block." `ApplicationRuntimeNotifier::completion_ready` calls `publish`, and `publish` takes `bundle.lock()`, a blocking acquire. That same slot is held deliberately across a manual backup: `execute_manual_backup_command` keeps the guard through `submit_and_wait` bounded by `MANDATORY_BACKUP_TIMEOUT`, five minutes. The notifier is handed to all four engine workers -- live, git, quota and reminder -- and `run_worker` calls it synchronously on the worker thread between publishing a receipt and starting the follow-up. So a user pressing Backup now parks every worker that completes a refresh in that window: refreshes, follow-ups and health publications stop and the dashboard holds stale numbers for the length of the backup. A stall rather than a deadlock, because the maintenance worker never takes the bundle -- but it is the same shape this branch fixed in the engine, one layer up. **The repair is already half-built:** the struct carries a `pending: AtomicBool` for exactly the dropped-hint case, so `try_lock` plus setting `pending` on contention is what "lossy" already means here, and the next completion republishes. Prove it with a test that holds the slot on one thread and requires `completion_ready` to return within 100 ms, then restore `lock()` and watch it hang. Found independently by two critics; the lock, the trait sentence and the five-minute hold were each read here before recording. | `ApplicationRuntimeNotifier::publish` in `crates/app/src/application.rs`; `WorkerCompletionNotifier` in `crates/engine/src/worker.rs` |
| post-tag | **The one screen that shows the product's headline numbers decides their availability from a glyph.** `DashboardHeaderCard` in `dashboard-view.slint` carries `availability: root.tokens == "—" ? "unavailable" : "known"`, the same again for events, and `color: root.cost == "—" ? ...` for cost. `DesktopValueAvailability` has five states and `format_tokens` renders `Partial` as `162,790 (3/7)` -- a string that is not an em dash, so the card announces it **known** at full weight, indistinguishable from a complete count. This breaks two invariants at once: unavailable, partial and legitimate-zero must stay distinguishable everywhere they are shown, and every number must arrive by snapshot rather than be recomputed beside it. The projection already knows: `models-total-availability`, `projects-total-availability` and six more exist in `main.slint`, and the trend card one row away binds `availability: root.points[0].tokens-availability` correctly. Only the header card re-derives it. Change one formatter's em dash to a figure dash and every test stays green while the card starts calling missing data known. The fix is to carry the fact -- header availability properties set in `apply_dashboard` from `header.tokens().availability()` -- and delete the three comparisons; proven by projecting a `Partial` header and requiring the string `partial`, an assertion that cannot even be written today. Found independently by two critics. | `DashboardHeaderCard` in `crates/desktop/ui/views/dashboard-view.slint`; `fn apply_dashboard` in `crates/desktop/src/ui.rs` |
| post-tag | **The check that forbids short hang bounds only looks in `crates/**/tests/`, and the short bounds live under `src`.** `m0-scripts.Tests.ps1` filters with `Where-Object { $_.FullName -match '\\tests\\' }` before scanning for `Instant::now() + Duration::from_secs(N)`. Every `#[cfg(test)]` module inside `src` is therefore invisible to it. Confirmed by grep against the same pattern: `crates/desktop/src/controller.rs` carries two two-second deadlines, `crates/app/src/notification_tests.rs` one, and `crates/app/src/application_tests.rs` a five-second one -- the last of which this plan had not previously named. The check is green while the rule it enforces is broken in-tree, which is the third instance of that shape found this round. The repair is to drop the filter and scan every `*.rs` under `crates`; the red-first proof comes free, since the widened scan fails immediately on those four. Deliberate short bounds that measure latency need an explicit skip token written where the scan reads it, not an exclusion by directory. | the `\\tests\\` filter in `scripts/tests/m0-scripts.Tests.ps1` |
| **serious, open** | **The whole-tree animation scan reads only first-party sources, and the animation a refactor would reach for lives in the Slint crate.** `std-widgets`' `Spinner { indeterminate: true; }` is built on `SpinnerBase`, whose `spinner-base.slint` in `i-slint-compiler-1.17.1` reads `animation-tick()` **twice** -- verified in the vendored source. The door is already open: seventeen of the twenty-five views import from `std-widgets.slint`, `dashboard-view.slint` among them, so adding `Spinner` to that import is one word. Replace `ImportActivityDot` with a spinner -- the obvious simplification after reading the comment that says `animation-tick()` is dangerous -- and eager `if` instantiation reproduces the recorded incident, 97.7% of one core shown and 105.5% hidden, with the first-party scan finding **zero** matches. Zero fails loudly, which is the one mercy here; but delete the now-dead component too and the natural repair is to relax the test. The fix is a closed import set: extract every name imported from `std-widgets.slint` across the tree and require it to equal the list the product uses today, the same habit as the msgid arrays and `Get-ProductPackageExpectedFile`. Break it by adding `Spinner` to one view's import. Found by the Opus critic; missed by both Fable passes. | `the_only_animation_in_the_slint_tree_is_the_guarded_import_dot` in `crates/desktop/tests/recovery_ui_contract.rs` |
| **serious, open** | **Moving the animation rule to a whole-tree walk left the path rule behind on five files.** `recovery_ui_source_keeps_authority_bounded_and_accessible` still builds `combined` from five `include_str!` names and runs `path`, `filename`, `file-name`, `std::fs` and `rusqlite` against only those, while the tree has twenty-five sources. Twenty views -- `projects-view`, `sessions-view`, `history-view`, `models-view`, `activity-view`, `compact-widget-view` among them -- have no guard against an absolute path reaching the interface, and the invariant they would break is the first one in `CLAUDE.md`. This is exactly where the deferred Live Sessions and project-alias work is heading, since both want to name the repository a row belongs to. The fix is to run the forbidden list over the same from-disk collection the animation scan already builds and to report the offending file; the animation tokens leave that list, since `animates` owns them now and a byte list is what failed before. Break it by adding `in property <string> path;` to `sessions-view.slint`. Found by the Opus critic while auditing the repair I had just made. | the five-file `combined` in `crates/desktop/tests/recovery_ui_contract.rs` |
| **serious, open** | **Two ways a Pester stage still disappears quietly.** The composition pin asserts the suite's file name appears somewhere in `verify-m0.ps1`'s text, never that it reaches an `Invoke-PesterChecked` call -- so putting a `#` in front of a stage leaves the name present *inside that very comment*, the guard green, and the gate running seven suites while printing fifteen stages. The proof recorded when that pin was written was a **deletion** of the line, which is the one mutation a substring match does catch; commenting out is the one it does not. Separately the stage floor requires one passing test, so `-Skip` on four of five `It` blocks leaves `PassedCount = 1` and the stage green. And every stage records `id = "pester"` in the receipt, so `reports/verification-summary.json` cannot tell seven Pester stages from eight either. The repair removes the list rather than hardening the match: enumerate `scripts/tests/*.Tests.ps1` inside `verify-m0.ps1` itself and run each, so a suite that exists is run by construction; add `SkippedCount -eq 0` to the stage condition; record the suite's own name and its passed count. Break it by commenting out one stage and by skipping one `It`. Found by the Opus critic; the comment half was found by Fable too. | `verification runs every Pester suite that exists` in `scripts/tests/m0-scripts.Tests.ps1`; `Invoke-PesterChecked` in `scripts/verify-m0.ps1` |
| note | **Two decision records point at a package that was deleted, and one ledger row names a gate that does not exist.** ADR-078 explains why the tray is hand-written by saying the Slint `system-tray` feature "is scoped only to the separate M0 probe"; `tokenmaster-m0` is not among the workspace members -- verified, zero matches in `Cargo.toml` -- and `CLAUDE.md` records that probe as deleted. ADR-049 carries the same stale present tense. So a reader following the reasoning lands on a crate that is not there, and the property the sentence exists to explain now rests only on the absence of a feature flag nothing asserts. Separately `docs/FEATURE_PARITY.md` states that a `rejected` row passes only with a regression gate present, and one row's gate is "CLI/MCP allowlist tests" -- there is no CLI or MCP crate, and `allowlist` appears in **no** `.rs`, `.ps1` or `.toml` file in the repository. Following this repository's own precedent, the ADRs get a closing pointer rather than a rewrite: they are history, and ADR-015 set that shape. The ledger row either names a check that exists today or moves to `planned`. Found by the Opus critic. | ADR-078 and ADR-049 in `spec/DECISIONS.md`; the `rejected` rows in `docs/FEATURE_PARITY.md` |

## Direction, and where the idle cost is not

The trend card's area fill and its two-dot legend are **confirmed on a live window**: the
teal and blue series each carry a soft fill, and `tokens` and `cost` are named beside the
`Ready` pill. Both had been committed on their unit tests alone.

`docs/UI_REFERENCE.md` holds the owner's Dashboard mockup transcribed as a binding
specification. It is the standard: where the built product differs from it, the product is
wrong. Its second list -- the elements blocked on data the projection does not carry -- is
the more valuable half, because each entry is a fact the product already computes and
discards.

The UI stack is settled and was checked rather than assumed. Six research angles across
forums, issue trackers and shipped-product retrospectives, with the strongest claims put
through refutation. Verdict: keep Slint with the Skia renderer. Every alternative loses on
one hard requirement -- unavailable, partial and legitimate-zero must stay distinguishable
in the accessible label. iced has carried an open accessibility issue since 2020, GPUI is
invisible to JAWS and NVDA, Tauri and Dioxus are a WebView, Xilem is alpha. egui is the only
real fallback and it has no hot reload at all.

**Turn on `slint/live-preview`.** Measured on this repository: generated Rust falls from
15,997,690 bytes to 296,507 -- fifty-four times smaller -- with an identical API surface,
and a `tokens.slint` edit stops costing a thirteen-second rebuild. It is a non-default
feature and must never reach a release build.

**The idle cost is closed, and it was never rendering.** Two defects held a core, and both
were found by measuring writes rather than by reading code -- three hypotheses taken from the
source were wrong first, one of them twice.

The first was a transaction boundary. Presence is recorded for every source a scan sees,
changed or not, and each source carried its own transaction: 8032 of 9564 journal frames landed
on `usage_source` across 237 distinct pages, one commit per file. Batching them cost 16.0 MB per
pass down to 1.31 MB.

The second was the shape of a hint. The watched root is the profile root, not the sessions
directory, and the tools that own that root write into it constantly -- of eight files changed
under a real `~/.codex` in ten minutes, seven were outside both source directories. Any such path
failed the whole targeted batch, which became a full walk of all 4016 sources. Skipping paths
that can never be usage data -- while still reconciling a path whose file merely disappeared --
removed the walk.

Measured end to end on the live 816 MB archive, same script both times: journal over 120 seconds
fell from 165 MB to 4.91 MB, processor time from 151.8-171.1 seconds per 180 to 27.4 per 120, and
the process now reaches zero processor use about forty seconds after launch instead of holding a
core indefinitely.

Threads already carry names; the standard library sets them. A probe that reports otherwise is
asking `GetThreadDescription` for the wrong access right.

## Abort rules

These are binding. They exist because this project produced 543 commits and zero
releases, and every stop condition it had was judged by the agent producing the work.

- **Migration ladder.** If the byte-exact schema diff does not converge within one extra
  day, keep the ladder and tag anyway. Dead migration code is a cost optimisation; it
  never holds the first release hostage.
- **Cache-write columns and the ladder collapse share one commit window.** Collapsing
  first and adding the columns later rebuilds a ladder of one.
- **Cold-import wall time is not a gate.** No normative requirement states one, and
  ADR-093 records that the WhereMyTokens comparison is not a like-for-like workload.
  Keep the measured number as a baseline with a no-regression guard.
- **Two failed attempts at one construction** means pinning the invariant one level
  down, not a third attempt.
- **A defect found outside the current item** is recorded and deferred, never fixed
  inline.

Record an `AUDIT_HARDENING_LOOP` trigger and its disposition here before continuing.

## Deferred and external

Signing, the 24-hour soak, physical DPI/accessibility/screen-reader passes, and
authenticated clean-room evidence are outside this milestone. The soak harness was
deleted with the probe binary it measured; it must be rewritten against the packaged
executable before any soak is run.

`scripts/stranger-acceptance.ps1` is the P4 criterion made runnable: it extracts the shipped
ZIP where nothing was built, starts it, and reads the shell header and Today card out of the
accessibility tree while the first import runs. `scripts/measure-archive-cost.ps1` samples
archive growth, journal growth and processor time against the live archive. Both are
operator-run against built artifacts, and both declare DPI awareness before measuring --
without it a capture silently loses half the window, which once produced three recorded
defects that did not exist.

`scripts/validate-p3e-interactive.ps1` belongs here: it takes a built executable and a
built package, so it is an operator-run gate against a packaged artifact, not a CI step.
Its acceptance criteria are in `P3E_ACCEPTANCE.md`. Nothing invokes it automatically, and
nothing should.
